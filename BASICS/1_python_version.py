# python3 --version
#this command is directly used in cmd to know the version of the python. we can directly use it in python

# It is used to access system-specific parameters and functions
import sys
print (sys.version)
